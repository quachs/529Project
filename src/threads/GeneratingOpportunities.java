package threads;

/**
 * The three opportunities the generating thread can work in. BOOLEAN = boolean
 * retrival, RANKED = ranked retrival, ALLL = get all vocabulary.
 *
 * @author Sandra
 */
public enum GeneratingOpportunities {
    BOOLEAN, RANKED, ALL;
}
